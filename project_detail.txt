- Associate the More Info button with the show page (ok)
# Show page
  // Front-end
     - Layout (ok)

# Refactor
  - Routes (ok)

# Posts
  // Back-end
     - Model (ok)
     - Routes: new (ok), create (ok), show
     - Association: user (ok), bunny (ok)
  // Front-end
     - Views: new (ok), show

# Refactor seedDB using Promises (ok)
# Refactor seedDB using async/await (ok)
# Adding posts to seedDB (ok)

# Post Routes
  1. Edit
     (1) Router (ok)
     (2) Page (ok)
  2. Destroy (ok)

# Comments
  1. Model (ok)
  2. Routes
     (1) New (ok)
     (2) Create (ok)
     (3) Edit (ok)
     (4) Update (ok)

     // Middleware: isCommentAuthor (ok)
     (5) Destroy (ok)
  3. Views (ok)
     (1) Comment.author population (ok)

# Flash message


// Issues:
   # Prevent the user from create another bunny if he/she already has one
   # Both posts & comments: an empty line typed in the textarea won't be displayed in the show page
   # After one login failure, a login success will redirect the user to the login page
   # mt-15vh --> mt-13vh (ok)
   # Where to display bunny's description?